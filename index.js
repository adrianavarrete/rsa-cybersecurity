const fs = require('fs');
const a = "hello";
const privateKey = require('./PrivateKey');
const publicKey = require('./PublicKey');

function generateKeys() {

    return privateKey;
}


exports.a = a;